import { ComponentWrapper, MainWrapper, Spacer, TextInputSearch } from '../../../components'
import { height } from 'react-native-dimension';
import { MapFilteredComponent } from './components';

const MapFiltered = () => {
    return (
        <MainWrapper>
            {/* <Spacer heightht={height(3.5)} />
            <ComponentWrapper>
                <TextInputSearch />
                <Spacer height={height(1.5)} />
                <MapFilteredComponent />
            </ComponentWrapper> */}
        </MainWrapper>
    )
}

export default MapFiltered